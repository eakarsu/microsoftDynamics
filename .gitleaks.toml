title = "microsoftDynamics secret scanning"

[extend]
useDefault = true

[[allowlists]]
description = "Documented placeholders and lockfile integrity hashes"
paths = [
  '''(^|/)\.env\.example$''',
  '''(^|/)package-lock\.json$'''
]
