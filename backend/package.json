{
  "name": "dynamics365-backend",
  "version": "1.0.0",
  "description": "Governed lead qualification and opportunity conversion API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "migrate": "node scripts/migrate.js",
    "migrate:check": "node scripts/migrate.js --check",
    "bootstrap": "node scripts/bootstrap.js",
    "create-admin": "node scripts/bootstrap.js",
    "seed": "node seed.js",
    "test": "node --test --test-concurrency=1 test/*.test.js",
    "check": "node scripts/check.js"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "pg": "^8.22.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
